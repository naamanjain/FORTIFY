import { AppShell } from '../components/AppShell'
import { PageChrome } from '../components/PageChrome'
import { Icon } from '../components/Icon'
const nav=[{id:'attention',label:'Attention',path:'/attention',icon:'alert' as const},{id:'personnel',label:'Personnel',path:'/personnel',icon:'person' as const},{id:'units',label:'Units',path:'/units',icon:'units' as const},{id:'trends',label:'Trends',path:'/trends',icon:'trend' as const},{id:'data',label:'Data & Signals',path:'/data/signals',icon:'data' as const},{id:'reviews',label:'Reviews',path:'/reviews',icon:'review' as const},{id:'governance',label:'Governance',path:'/governance',icon:'audit' as const}]
function push(p:string){history.pushState({},'',p);dispatchEvent(new PopStateEvent('popstate'))}
export default function Restricted({title='Restricted view',subtitle='This surface is available only to the appropriate authorized role and purpose.'}:{title?:string;subtitle?:string}){return <AppShell navItems={nav} activePath="" query="" searchResults={[]} searchOpen={false} onQueryChange={()=>{}} onSearchSelect={()=>{}} onNavigate={push} notificationsOpen={false} onToggleNotifications={()=>{}}><PageChrome eyebrow="ACCESS" title={title} subtitle={subtitle}><div className="restricted-panel"><Icon name="lock" size={28}/><h2>Restricted information</h2><p>Your current access context does not permit this information. No hidden values are exposed through this state.</p></div></PageChrome></AppShell>}
